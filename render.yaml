services:
  - type: web
    name: smart-education-ml-service
    env: node
    region: oregon
    plan: free
    buildCommand: pip install -r ml-service/requirements.txt
    startCommand: node ml-service/server.js
    envVars:
      - key: NODE_ENV
        value: production
