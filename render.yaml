services:
  - type: web
    name: smart-education-ml-service
    env: docker
    region: oregon
    plan: free
    dockerfile: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
