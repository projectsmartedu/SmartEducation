# Smart Education ML Service

Separate microservice for AI/ML model predictions.

## 📋 Folder Structure

```
ml-service/
├── server.js                 # Express server (main entry point)
├── ml_inference.py          # Python inference engine
├── package.json             # Node.js dependencies
├── requirements.txt         # Python dependencies
├── .env.example             # Environment variables template
├── models/                  # Trained model files (generated by training)
│   ├── risk_pipeline_v2.pkl
│   ├── risk_features_v2.pkl
│   ├── revision_planner_model.pkl
│   ├── revision_features.pkl
│   └── model_metadata.pkl
└── scripts/
    ├── train_models.py      # Train ML models
    └── test_model.py        # Test predictions locally
```

## 🚀 Quick Start

### 1. Install Dependencies

**Node.js:**

```bash
cd ml-service
npm install
```

**Python:**

```bash
pip install -r ml-service/requirements.txt
```

### 2. Train Models

```bash
python3 ml-service/scripts/train_models.py
```

This generates 5 .pkl files in `ml-service/models/`

### 3. Test Locally

```bash
python3 ml-service/scripts/test_model.py
```

### 4. Run ML Service

```bash
node ml-service/server.js
```

Service runs on `http://localhost:5001`

## 📊 API Endpoints

### Risk Prediction

**POST** `/api/risk/predict`

```json
{
  "prior_failures": 2,
  "study_time": 5,
  "absences": 8,
  "parent_edu": 4,
  "family_support": 3,
  "health": 4,
  "internet": 1,
  "activities": 1,
  "travel_time": 2,
  "age": 18,
  "paid_support": 0
}
```

**Response:**

```json
{
  "riskScore": 0.62,
  "category": "MEDIUM",
  "intervention": "Regular monitoring...",
  "confidence": 0.88
}
```

### Revision Urgency

**POST** `/api/revision/mindmap`

```json
{
  "studentId": "STU-001",
  "topicProgress": [
    {
      "name": "Linear Equations",
      "mastery": 0.35,
      "days_since_studied": 21,
      "attempts": 1,
      "last_score": 45,
      "practice_hours": 0.5
    }
  ]
}
```

**Response:**

```json
{
  "studentId": "STU-001",
  "nodes": [...],
  "edges": [...],
  "generatedAt": "2026-03-03T14:30:00"
}
```

## 🌐 Deployment on Render

1. Create new Render Web Service
2. Connect GitHub repo
3. **Start Command:** `node ml-service/server.js`
4. Set environment variable: `ML_SERVICE_URL=<your-render-url>`

## 📈 Model Performance

- **Risk Model:** Test AUC 0.9013 ✅
- **Revision Model:** Test AUC 0.9166 ✅

## 🔧 Troubleshooting

**"Model files not found"**

```bash
python3 ml-service/scripts/train_models.py
```

**"Python subprocess error"**

- Ensure Python 3.8+ is installed
- Verify `requirements.txt` packages are installed
- Check Render build logs

## 📝 License

MIT
